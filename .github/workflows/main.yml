name: SMTP Mail Test
on:
  schedule:
    - cron: '0 21 * * 2'  # JST 火曜 6:00
  workflow_dispatch:

jobs:
  send-mail:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install dependencies
        run: pip install google-auth google-auth-httplib2 google-api-python-client pandas openpyxl

      # ── STEP 1: Google Drive から Excel をダウンロード ──
      - name: Download Excel from Google Drive
        env:
          GOOGLE_SERVICE_ACCOUNT_JSON: ${{ secrets.GOOGLE_SERVICE_ACCOUNT_JSON }}
          EXCEL_FILE_ID: ${{ secrets.EXCEL_FILE_ID }}
        run: |
          python - <<EOF
          import os, io, json
          from google.oauth2 import service_account
          from googleapiclient.discovery import build
          from googleapiclient.http import MediaIoBaseDownload

          sa_info = json.loads(os.environ["GOOGLE_SERVICE_ACCOUNT_JSON"])
          creds = service_account.Credentials.from_service_account_info(
              sa_info,
              scopes=["https://www.googleapis.com/auth/drive.readonly"]
          )
          drive = build("drive", "v3", credentials=creds)

          request = drive.files().get_media(fileId=os.environ["EXCEL_FILE_ID"])
          buffer = io.BytesIO()
          downloader = MediaIoBaseDownload(buffer, request)
          done = False
          while not done:
              _, done = downloader.next_chunk()

          with open("/tmp/data.xlsx", "wb") as f:
              f.write(buffer.getvalue())
          print("Download complete: /tmp/data.xlsx")
          EOF

# ── STEP 2: Pandas で絞り込み・整形 ────────────────
      - name: Process with Pandas
        run: |
          python - <<EOF
          import pandas as pd
          import json
          from datetime import datetime, timedelta

          df = pd.read_excel("/tmp/data.xlsx", sheet_name=0, header=1)

          # 実行日（火曜）の前週月曜〜日曜を算出
          today      = datetime.today().date()
          last_mon   = today - timedelta(days=today.weekday() + 7)  # 前週月曜
          last_sun   = last_mon + timedelta(days=6)                 # 前週日曜
          print(f"抽出範囲: {last_mon} 〜 {last_sun}")

          # 希望納期を日付型に変換して絞り込み
          df["希望納期"] = pd.to_datetime(df["希望納期"], errors="coerce").dt.date
          filtered = df[(df["希望納期"] >= last_mon) & (df["希望納期"] <= last_sun)].copy()

          print(f"該当件数: {len(filtered)}件")

          if len(filtered) == 0:
              with open("/tmp/result.json", "w") as f:
                  json.dump([], f)
              print("該当なし。スキップ。")
              exit(0)

          # 担当者・納入先・希望納期 の組み合わせでグループ化
          groups = []
          for (staff, delivery_to, delivery_date), group in filtered.groupby(["担当者", "納入先", "希望納期"]):
              orders = group["顧客注文番号"].astype(str).tolist()
              details = []
              for _, row in group.iterrows():
                  details.append(
                      f"{row['製品詳細']} "
                      f"{int(row['販売単価'])} x {int(row['数量'])} = {int(row['販売価格'])}"
                  )
              groups.append({
                  "担当者": staff,
                  "注文番号リスト": orders,
                  "希望納期": str(delivery_date),
                  "納入先": delivery_to,
                  "明細リスト": details,
              })

          with open("/tmp/result.json", "w", encoding="utf-8") as f:
              json.dump(groups, f, ensure_ascii=False)
          print(f"Process complete: {len(groups)}グループ → /tmp/result.json")
          EOF

# ── STEP 3: 担当者ごとに個別メール送信 ─────────────
      - name: Send Mail
        env:
          SMTP_SERVER: ${{ secrets.SMTP_SERVER }}
          SMTP_PORT: ${{ secrets.SMTP_PORT }}
          SMTP_USER: ${{ secrets.SMTP_USER }}
          SMTP_PASS: ${{ secrets.SMTP_PASS }}
          STAFF_MAIL_MAP: ${{ secrets.STAFF_MAIL_MAP }}
        run: |
          python - <<EOF
          import os, smtplib, json
          from email.mime.text import MIMEText

          mail_map = {}
          for line in os.environ["STAFF_MAIL_MAP"].splitlines():
              line = line.strip()
              if "," in line:
                  name, address = line.split(",", 1)
                  mail_map[name.strip()] = address.strip()

          with open("/tmp/result.json", encoding="utf-8") as f:
              groups = json.load(f)

          if len(groups) == 0:
              print("該当なし。メール送信スキップ。")
              exit(0)
              
          # CCはSMTP_USERを使用
          cc_address = os.environ["SMTP_USER"]
          
          for g in groups:
              staff       = g["担当者"]
              orders      = g["注文番号リスト"]
              delivery    = g["希望納期"]
              delivery_to = g["納入先"]
              details     = g["明細リスト"]

              to_address = mail_map.get(staff)
              if not to_address:
                  print(f"⚠ {staff} のメールアドレスが見つかりません。スキップ。")
                  continue

              # 件名は最初の注文番号を使用
              subject = f"【自動送信】納品のご連絡（注文番号：{orders[0]}）"

              # 注文番号リストを改行で並べる
              orders_text  = "\n".join(orders)
              details_text = "\n".join(details)

              mail_body = (
                  "いつもお世話になっております。\n"
                  "下記の通り、納品内容をご連絡いたします。\n"
                  "\n"
                  "■注文番号\n"
                  + orders_text +
                  "\n\n"
                  "■納品予定日\n"
                  + delivery +
                  "\n\n"
                  "■納入先\n"
                  + delivery_to +
                  "\n\n"
                  "■納品内容\n"
                  + details_text +
                  "\n\n"
                  "ご確認のほどよろしくお願いいたします。\n"
                  "※本メールはシステムより自動送信されています。\n"
                  "---"
              )

              try:
                  msg = MIMEText(mail_body, "plain", "utf-8")
                  msg["Subject"] = subject
                  msg["From"]    = os.environ["SMTP_USER"]
                  msg["To"]      = to_address
                  msg["Cc"]      = cc_address
                  
                  port = int(os.environ["SMTP_PORT"])
                  server_addr = os.environ["SMTP_SERVER"]

                  if port == 465:
                      server = smtplib.SMTP_SSL(server_addr, port)
                  else:
                      server = smtplib.SMTP(server_addr, port)

                  server.login(os.environ["SMTP_USER"], os.environ["SMTP_PASS"])
                  server.sendmail(msg["From"], [to_address], msg.as_string())
                  server.quit()
                  print(f"送信完了: {staff} <{to_address}>  注文{len(orders)}件")

              except Exception as e:
                  print(f"Error ({staff}): {e}")
                  raise
          EOF
